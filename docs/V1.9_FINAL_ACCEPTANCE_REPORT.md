# V1.9 — Final Acceptance Report (Candidate A)

**تاریخ:** 2026-09-26 · **فاز:** CI Run Verification & Final Acceptance

---

## 1. Scope

V1.9 Candidate A کامل طبق `DEC-039` (Module UI · Stage UI · Stage Weight · Stage Approval · WBS Checklist · Progress Report بر پایهٔ `approved_amount`) با قواعد `DEC-040` (حذف از گزارش stage-based + شمارش علنی) و `DEC-041` (برچسب «مبلغ تأییدشدهٔ مراحل»). **صفر Scope Expansion.**

## 2. Implementation Baseline

* فاز Implementation: `feat(v1.9)` — 39 فایل · zero migration · گزارش: `docs/V1.9_IMPLEMENTATION_REPORT.md`
* Verification & Hardening: 0 Blocker · 0 Defect · 2 Warning (pre-existing/out-of-scope) — `docs/V1.9_VERIFICATION_HARDENING_REPORT.md`

## 3. Verification Baseline

* Local Full Suite (اجرای واقعی قبل از Commit): **256 passed · 735 assertions · 0 failed · 2 deprecated**
* V1.9 subset: 53 passed · 144 assertions · 0 failed
* Pint: PASS · `npm ci` PASS · `npm run build` PASS · `manifest.json` PRESENT
* `git diff -- database/migrations` = خالی (قبل از Commit)

## 4. Commit SHA

```text
Commit:  1c2b079f9e7ed7905a673e56a5548efda8d5a03a (short: 1c2b079)
Branch:  main
Parent:  2bed1d5
Message: feat(v1.9): implement candidate A — module/stage/approval/WBS UI + approved_amount report
Changed: 39 files · +3649 / -8
Migrations in commit: NONE (verified — no database/migrations path in commit)
Stale artifact v19_out.txt: NOT committed
```

## 5. CI Run ID

```text
Run ID:      36231957880
Run Number:  6
Workflow:    .github/workflows/ci.yml
Event:       push (branch: main)
URL:         https://github.com/hadi78m/tms/actions/runs/36231957880
Created:     2026-09-26T09:10:19Z · Completed: 2026-09-26T09:11:10Z (≈51s)
```

## 6. CI Results (evidence: GitHub Actions API)

**Run:** `status=completed · conclusion=success` · `head_sha = 1c2b079f9e7e...` = HEAD tested ✅

**Job:** `Tests (PHP 8.4 / PostgreSQL 18)` — `completed / success` — همهٔ ۲۰ step:

| Step | Result |
|---|---|
| Initialize containers (postgres:18) | ✅ success |
| Checkout | ✅ success |
| Setup PHP 8.4 | ✅ success |
| Install Composer dependencies (locked) | ✅ success |
| **Gate 1 — DB identity `tms_testing` + PG major 18** | ✅ **PASS** |
| Migrate testing database | ✅ success |
| **Gate 2 — migration status (32 ran, 0 pending)** | ✅ **PASS** |
| Setup Node 22 · npm ci | ✅ success |
| Build frontend assets | ✅ success |
| **Frontend build gate — manifest exists** | ✅ **PASS** |
| **Run test suite (T-6 gate active)** | ✅ **PASS** |
| Mask credentials · Upload summary · Post steps | ✅ success |

*توجه:* لاگ‌های تفصیلی Job نیازمند دسترسی admin هستند (API 403 برای anonymous) — اما وضعیت هر step و conclusion کل run از API عمومی Actions استخراج و در بالا ثبت شد. شمارش دقیق تست در CI از لاگ عمومی قابل استخراج نبود؛ **عدد Local (256/735) به‌عنوان CI ثبت نشد** — تنها `Run test suite = success` (که خودش `0 failed` را الزامی می‌کند، چون Pest بدون شکست exit 0 نمی‌دهد).

## 7. Test Results

| مجموعه | Local (قبل از Commit) | CI |
|---|---|---|
| Full suite | 256 passed · 735 assertions · 0 failed · 2 deprecated | `Run test suite` step = success (exit 0 ⇒ 0 failed) |
| V1.9 subset | 53 passed · 144 assertions | شامل در همان run |

## 8. Database Safety

```text
Production DB contacted by CI ......... NO (workflow فقط tms_testing را می‌سازد/متصل می‌شود)
Production DB mutation (local phases) . NO (فقط SELECT/current_database/migrate:status)
Test DB ............................... tms_testing از طریق phpunit.xml + T-6 Gate فعال
```

## 9. Migration Status

```text
database/migrations diff .............. EMPTY (قبل و بعد از Commit)
Migrations ........................... 32/32 Ran · 0 Pending (CI Gate 2 PASS)
New/modified migration in commit ...... NONE
```

## 10. Frontend Verification

```text
npm ci ............ PASS (0 vulnerabilities)
npm run build ..... PASS (built in 2.14s — local) · CI step PASS
manifest.json ..... PRESENT (local test -f PASS · CI gate PASS)
Build artifacts ... committed نیست (git status پس از build تمیز)
```

## 11. Security / Authorization Verification

* 18 مسیر V1.9 با `role:` middleware + گاردهای سرویس (supervisor-only decide · mode-based propose).
* 9 FormRequest با bounds سمت سرور؛ `weight` در update ماژول `prohibited`.
* CSRF روی همهٔ mutationهای POST؛ ownership check آیتم↔فاز (404).
* Contractor در تمام مسیرهای مدیریتی جدید 403 (تست‌شده).
* Audit: همهٔ mutationها از سرویس‌ها با `AuditServiceInterface` در همان تراکنش — صفر swallow.

## 12. DEC-038 Verification

* T-3=A: هیچ `task_rejected` جدید ایجاد نشد. ✅
* T-5=A: هیچ مسیر حذف Project/Module/Stage/Phase در routeها وجود ندارد (audit §7 گزارش Verification). ✅
* T-1 KEEP: زنجیرهٔ supersede و گاردهای سرویس دست‌نخورده. ✅

## 13. DEC-039 Verification

* Candidate A کامل: ۶ قابلیت تأییدشده پیاده شد؛ صفر Migration؛ Domain Services مالک Business Rules؛ هیچ قابلیتی خارج از فهرست (بدون Performance Record/Ledger/Financial/API/AI/...). ✅

## 14. DEC-040 Verification

* تسک‌های `module_stage_id IS NULL` از تجمیع مرحله‌ای حذف و `excluded_no_stage_tasks` صریحاً گزارش می‌شود؛ تسک‌ها در DB و گزارش‌های task-level (وزن کل، CSV، فهرست) دست‌نخورده‌اند — تست `test_stageless_task_is_excluded_from_stage_aggregation_but_counted`. ✅

## 15. DEC-041 Verification

* KPI با برچسب «مبلغ تأییدشدهٔ مراحل» رندر می‌شود و «آماده پرداخت» حذف شده — تست `test_label_is_mablagh_taeed_shodeye_marahel`. ✅

## 16. Financial Boundary Verification

* جستجوی کل مخزن برای payment/payout/invoice/payable/settlement: تنها ارجاعات، کامنت‌های مستنداتیِ *ردکنندهٔ* برداشت مالی هستند. `approved_amount` عملیاتی/درصدی وزنی باقی مانده؛ هیچ محاسبهٔ مالی اضافه نشده. ✅

## 17. Remaining Warnings

* **W-1** — Authorization همچنان route-middleware محور (TD-2، بدون Policy class) — pre-existing؛ اصلاح نیازمند Decision مالک است.
* **W-2** — فیلد اختیاری DEC-013 (اتصال Task به Stage در فرم Create Task) انجام نشد — خارج از Scope DEC-039.
* هیچ‌کدام blocker نیستند و در این فاز تغییر داده نشدند.

## 18. Final Acceptance

```text
V1.9 Candidate A      = ACCEPTED
CI Verification       = PASS (Run 36231957880 · commit 1c2b079 · all gates green)
Production DB Mutation = NONE
Migration Changes      = NONE
Scope Expansion        = NONE
```

```text
V1.9 = FINAL ACCEPTED
NEXT PHASE = NOT STARTED
```
