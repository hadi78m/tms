# V1.8 — T-7 TARGETED CI PHP 8.4 FIX & RE-RUN REPORT

**تاریخ:** 2026-09-23
**محدوده:** فقط fix هدفمند PHP matrix + add-mask — صفر تغییر کد/تست/migration/lock

---

## 1. Pre-flight Findings

| بررسی | یافته |
|---|---|
| `composer.json` → `require.php` | `^8.3` (حداقل، نه سقف) |
| `laravel/framework` (lock) | `v13.31.0` requires `php: ^8.3` |
| `composer.lock` platform | `{"php":"^8.3"}` · content-hash `39291217197940c219e2318345c49235` |
| پکیج‌های requiring PHP 8.4+ | **۴۸ پکیج** — `symfony/* v8.1.x` (>=8.4.1)، `pestphp/pest v5.2` (^8.4)، `phpunit 13.3.3`، `paratest v7.24`، `sebastian/*` |
| `platform-overrides` | خالی |
| workflow matrix | `php: ['8.3']` |
| git state | clean (به‌جز گزارش فاز قبل) · HEAD = `a0df128` |

## 2. Composer Lock / PHP Mismatch

مجموعهٔ قفل‌شده با پلتفرمی resolve شده که PHP ≥8.4 را در اختیار دارد (محیط محلی PHP 8.5.6
است). `composer.json` فقط **حداقل** `^8.3` می‌خواهد؛ هیچ سقفی برای 8.4 وجود ندارد.

## 3. Decision Gate

```text
composer.json permits PHP 8.4?           YES (^8.3 شامل 8.4 است؛ Laravel v13.31.0 هم ^8.3)
composer.lock requires PHP 8.4?          YES (۴۸ پکیج — ۱۹ Problem در run اول)
Laravel 13 compatible with PHP 8.4?      YES (^8.3 constraint = بازهٔ 8.3 ≤ x < 9.0)

⟹ CI PHP matrix: 8.3 → 8.4  (تنها تغییر مجاز؛ بدون ignore-platform-reqs)
```

## 4. Exact Workflow Change

```diff
-        # composer.json requires "php": "^8.3" — 8.3 is the lowest supported version.
-        php: ['8.3']
+        # composer.lock was resolved with platform php ^8.3 and contains 48
+        # locked packages requiring php >=8.4 (symfony/* v8.1, pest v5, phpunit).
+        # PHP 8.4 satisfies both composer.json (^8.3) and the locked set.
+        php: ['8.4']
```

+ یک step کوچک `::add-mask::` برای DB_PASSWORD و APP_KEY dummy (تغییر حداقلی مجاز §۶).

## 5–7. Files Changed / Commit / Push

```text
Files changed: .github/workflows/ci.yml (8+/2-) + docs/V1.8_T7_CI_RUN_VERIFICATION_REPORT.md (گزارش قبلی)
Commit SHA:    31936a1 — "ci: align PHP runtime with locked dependencies"
Push status:   SUCCESS (a0df128..31936a1 → origin/main)
composer.json / composer.lock / app / tests / migrations: UNTOUCHED
```

## 8. New GitHub Actions Run

```text
Run ID:   35830322842   ·   Job ID: 107081165722   ·   Commit: 31936a1   ·   Branch: main
Started:  2026-09-23T07:10:16Z  ·  Duration ~35s
```

## 9. Step-by-Step Run Results

| Step | Result |
|---|---|
| Checkout / Setup PHP (8.4.25) | ✅ |
| **Install Composer dependencies** | ✅ **PASS** (Composer Gate §۱۱ برقرار — دیگر هیچ `requires php >=8.4` وجود ندارد) |
| **Gate 1** | ✅ **PASS** — لاگ واقعی: `current_database = tms_testing` · `PostgreSQL major version = 18` |
| Migrate testing database | ✅ PASS — ۳۲ migration روی سرویس PostgreSQL CI |
| **Gate 2** | ❌ **FAILURE** — ریشهٔ دقیق پایین |
| Run test suite / Mask | ⏭ skipped |

## 10–13. Gates

- **Composer = PASS** · **Gate 1 = PASS** (اطلاعات واقعی runner) · **Migrate = PASS**
- **Gate 2 = FAIL (اصلاح‌پذیرِ خودِ اسکریپت gate، نه ناهم‌خوانی migration):**
  - لاگ واقعی نشان می‌دهد **هر ۳۲ migration `[1] Ran`** هستند و **صفر Pending** —
    وضعیت مخزن کاملاً صحیح است (تأیید محلی: 32 Ran / 0 Pending).
  - **ریشهٔ شکست:** `PENDING=$(echo "$STATUS" | grep -cE '\] Pending ')` — وقتی
    `grep -c` **صفر match** پیدا کند exit code **1** برمی‌گرداند؛ step با
    `bash -e -o pipefail` اجرا می‌شود ⇒ assignment درستِ صفر، اسکریپت را بدون هیچ
    پیام خطایی قطع می‌کند (`##[error]Process completed with exit code 1` بلافاصله
    بعد از خروجی ۳۲ ردیف Ran، بدون چاپ `migrations ran = …`).
  - این **باگ خودِ اسکریپت gate** است (طبقهٔ A)، نه ناهم‌خوانی migration count.

## 14. Test Result

```text
Tests = NOT EXECUTED (Gate 2 مسدودشان کرد)
```

## 15. Production Safety Verification

```text
Production DB tms was NEVER used — PASS
تنها DB در کل اجرا: tms_testing (سرویس CI) · صفر dbname=tms / DB_DATABASE=tms
```

## 16. Secret / Logging Review

```text
add-mask step اضافه شد (فعلاً skipped چون Gate 2 اول fail شد).
هیچ secret واقعی در لاگ نیست — فقط dummy سرویس CI (که حالا mask می‌شود).
```

## 17. Failure Classification

```text
First failing step:  Gate 2 — Verify migration status
Exact error:         ##[error]Process completed with exit code 1 (بی‌پیام — رفتار bash -e)
Root cause:          grep -c با صفر match → exit 1 → pipefail کل step را می‌کُشد؛
                     دادهٔ واقعی (32/0) صحیح بود
Affected layer:      A — Workflow configuration (اسکریپت gate)
Production touched?  NO
Minimal next action: در Gate 2 از `grep -c ... || true` (یا پارامتر grep امن) استفاده
                     شود؛ فقط منطق count، بدون تغییر invariant (32/0).
```

## 18. Final Gate

```text
T-7 CI RUN FAILED — DIAGNOSTIC FOLLOW-UP REQUIRED
   (Gate 2 script bug — repair candidate مشخص و یک‌خطی؛ با تأیید مالک در فاز بعد)
```
