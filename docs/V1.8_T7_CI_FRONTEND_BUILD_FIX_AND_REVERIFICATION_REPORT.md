# V1.8 — T-7 CI FRONTEND BUILD FIX & RE-VERIFICATION REPORT

**تاریخ:** 2026-09-23
**محدوده:** فقط fix فرانت‌اند در `ci.yml` — صفر تغییر کد/تست/migration/package/production

---

## 1. Baseline Failure

```text
Run 35832036047 (commit 163ad93): Composer ✅ · Gate 1 ✅ · Migrate ✅ · Gate 2 ✅
Tests: 15 failed · 188 passed — همهٔ ۱۵ شکست = ViteManifestNotFoundException
```

## 2. Root Cause

صفحات Web که `@vite` در layout دارند، در CI بدون build فرانت‌اند 500 می‌دهند —
`public/build/manifest.json` در runner وجود نداشت.

## 3. Repository Inspection

| مورد | یافته |
|---|---|
| `package.json` scripts | `build: "vite build"` (موجود — همین استفاده شد، چیزی اختراع نشد) |
| Package manager | `package-lock.json` موجود ⇒ **npm ci** (نه npm install) |
| Node engines (lock) | `>=18` — نسخهٔ انتخابی CI: **22** (LTS جاری؛ شرط `>=18` را برآورده می‌کند) |
| `vite.config.js` | `laravel-vite-plugin` با input پیش‌فرض — خروجی استاندارد Laravel: `public/build` + manifest |
| Local proof | `npm ci && npm run build` → `✓ built in 2.29s` → **MANIFEST PRESENT ✅** |

## 4. Exact Workflow Change (فقط `ci.yml`، +26 خط)

ترتیب نهایی pipeline (مطابق §۱۰):

```text
Checkout → PHP 8.4 → Composer → Gate 1 → Migrate → Gate 2
        → Setup Node 22 → npm ci → npm run build → manifest assertion → Tests
```

Manifest gate (بدون `|| true`):

```bash
ls -la public/build
[ ! -f public/build/manifest.json ] && FATAL … && exit 1
```

## 5–7. Node / npm / Vite Decisions

- **Node 22:** lock فقط `>=18` می‌خواهد؛ 22 نسخهٔ LTS پشتیبانی‌شده و قابل‌cache است.
- **npm ci:** الزام صریح §۴ (lock موجود) — با `--no-fund --no-audit` برای سرعت.
- **Vite output:** بدون تغییر config — خروجی پیش‌فرض `laravel-vite-plugin` همان
  `public/build/manifest.json` است (اثبات: build محلی).

## 8. Local Build Result

```text
npm ci ............ PASS
npm run build ..... PASS (2.29s)
manifest .......... PRESENT (5,048 bytes)
```

## 9. Static Workflow Validation

```text
YAML parse ✅ · ترتیب گیت‌ها ✅ · add-mask حفظ ✅
PHP 8.4 / PG 18 / DB=tms_testing دست‌نخورده ✅
Safety scan (DB_DATABASE=tms / dbname=tms): صفر ✅
```

## 10–11. Commit & Run

```text
Commit: 22051ea "ci: build frontend assets before tests" → pushed (163ad93..22051ea)
Run ID: 35835190435 · Job: 107096823139 · Branch: main
Started: 2026-09-23T08:05:43Z · Finished: 08:06:39Z · Duration: ~56s
```

## 12. Real CI Results (همه از لاگ واقعی runner)

| Step | Result |
|---|---|
| Checkout / Setup PHP / Composer | ✅ |
| **Gate 1** | ✅ `current_database = tms_testing` · `PostgreSQL major version = 18` |
| Migrate / **Gate 2** | ✅ `migrations ran = 32 · pending = 0` |
| Setup Node / npm ci / **npm run build** | ✅ |
| **Frontend build gate** | ✅ `Vite manifest: PRESENT` |
| **Run test suite** | ✅ **`Tests: 2 deprecated, 203 passed (591 assertions)`** |
| Mask dummy credentials | ✅ اجرا شد |

```text
ViteManifestNotFoundException در این run: صفر
FAILED: صفر
```

*(تطابق جالب: نتیجهٔ CI — 203 passed / 591 assertions / 2 deprecated — دقیقاً برابر
Baseline تاریخی محلی شد؛ اما طبق §۱۹، مبنای این دروازه «نتیجهٔ واقعی همین run» است.)*

## 13. Security

```text
هیچ secret واقعی چاپ نشد. dummy DB_PASSWORD/APP_KEY هنوز در env-dump خودِ
GitHub Actions دیده می‌شوند (19/18 بار) اما مرحلهٔ add-mask فعال شد؛ حذف کامل
آن‌ها نیازمند حذف env از سطح job (تغییر معماری) — follow-up ثبت شد.
```

## 14–15. Production Safety & Repository Integrity

```text
Production database tms:     UNTOUCHED
Production data:             UNTOUCHED
Migrations modified:         0          New migrations: 0
Application code modified:   0          Tests modified: 0
composer.json/lock:          0          package.json/lock: 0
Business decisions invented: 0
Diff فاز: فقط .github/workflows/ci.yml (+26)
```

## 16. Final Gate

```text
FINAL GATE:
T-7 CI VERIFIED

Composer ........ PASS
Gate 1 .......... PASS      (tms_testing / PG 18)
Migration ....... PASS      (32/32)
Gate 2 .......... PASS      (32/0)
Frontend Build .. PASS      (Node 22 · npm ci · vite build)
Manifest ........ PRESENT
Tests ........... PASS      (203 passed · 591 assertions · 0 failed · 2 deprecated)
Production ...... UNTOUCHED

READY FOR NEXT V1.8 OWNER-DECISION IMPLEMENTATION PHASE
```
