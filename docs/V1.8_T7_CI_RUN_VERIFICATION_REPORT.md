# V1.8 — T-7 CI RUN VERIFICATION REPORT

**تاریخ:** 2026-09-23
**نوع فاز:** RUN → OBSERVE → VERIFY → REPORT — صفر تغییر (کد/تست/workflow/migration)

---

## 1. Run Identity

```text
Workflow ... CI  (.github/workflows/ci.yml)
Run ID ..... 35825473271
URL ........ https://github.com/hadi78m/tms/actions/runs/35825473271
Commit ..... a0df128 (feat: V1.8 schema, domain hardening, and T-7 safety-gated CI)
Branch ..... main (push trigger)
Job ID ..... 107066119176  ("Tests (PHP 8.3 / PostgreSQL 18)")
Started .... 2026-09-23T06:09:33Z · Completed: 2026-09-23T06:10:16Z
Final ...... FAILURE
```

## 2. Step Results

| Step | Result |
|---|---|
| Set up job / Initialize containers | success |
| Checkout | success |
| PHP (setup-php → 8.3.33) | success |
| **Composer install** | **FAILURE** |
| Gate 1 | **NOT REACHED** (skipped) |
| Migrate | NOT REACHED (skipped) |
| Gate 2 | NOT REACHED (skipped) |
| Tests | NOT REACHED (skipped) |
| Upload test summary | success |

PostgreSQL service container: **started successfully** (Initialize containers = success،
health-check سرویس گذشت — رد ردهٔ C و F فعال نشد).

## 3. Gate 1 Evidence

```text
Gate 1 = NOT REACHED
```

## 4. Migration Evidence

```text
Migration execution = FAIL (در واقع: NOT REACHED — قفل پیش از آن)
Gate 2 = NOT REACHED
```

## 5. Test Evidence

```text
Command:   NOT EXECUTED
Exit code: n/a  (Suite and all gates skipped)
```

آخرین Baseline محلی معتبر (یادآوری، نه جایگزین): 203 passed · 591 assertions · 0 failed · 2 deprecated.

## 6. Production Safety

```text
Production DB protection = PASS (by construction for this run)
```

هیچ مرحله‌ای به `tms` نزدیک نشد؛ تنها DB در کل workflow `tms_testing` سرویس CI است
(env لاگ‌شده: `DB_DATABASE: tms_testing` · `POSTGRES_DB: tms_testing`). هیچ
`DB_DATABASE=tms` / `dbname=tms` در اجرا ظاهر نشد.

## 7. Security Review

```text
Secret exposure = SUSPECTED (non-production dummy values)
```

- **هیچ credential واقعی تولید لو نرفت** — تنها مقادیر dummy سرویس CI در لاگ env
  چاپ شدند (`tms_ci_password` ×۱۱ و یک APP_KEY تست که خودم در workflow گذاشته‌ام؛
  نه از مخزن secret و نه متعلق به هیچ محیط واقعی).
- `.env` upload نشد · production credential چاپ نشد.
- **یافتهٔ بهبود آینده (خارج از این فاز):** با محرمانگی‌سازی env در GitHub
  (`env: { DB_PASSWORD: ${{ secrets... }} }` یا `::add-mask::`) این لاگ‌شدن dummyها
  هم حذف می‌شود.

## 8. Failure Classification

```text
Layer ............ D — PHP/runtime (composer platform mismatch)  [E adjacent]
Step ............. Install Composer dependencies (ramsey/composer-install, locked)
Observed ......... "Your lock file does not contain a compatible set of packages.
                    Please run composer update."
Log evidence ..... 19 Problem: symfony/* v8.1.x، pest v5.2، paratest v7.24 همگی
                    require php >=8.4.1 / ^8.4 — runner روی PHP 8.3.33 بود.
Repository code implicated?  NO (کد و migration دست‌نخورده)
Workflow change required?    YES (فقط شاخهٔ matrix: '8.3' → '8.4')
Root cause ................. composer.lock با PHP 8.4 resolve شده (env محلی: PHP 8.5.6
                    اجرا می‌کند و lock را با platform php>=8.4 ساخته)، در حالی که
                    composer.json فقط "^8.3" حداقل را می‌خواهد. CI درست طبق
                    composer.json عمل کرد؛ lock با واقعیت محلی هم‌خوان نیست.
```

طبق قانون §۱۱: **هیچ اصلاحی در این فاز انجام نشد** — اجرای ناموفق شاهد تشخیصی است.

## 9. Repository Integrity

```text
Migration files changed: 0        (commit a0df128: دقیقاً همان ۳۲ فایل)
Application files changed: 0      (در این فاز)
Test files changed: 0             (در این فاز)
Existing workflow changed: 0      (ci.yml از فاز قبل تغییر نکرد)
Production database changed: NO
Production data changed: NO
Only new file: docs/V1.8_T7_CI_RUN_VERIFICATION_REPORT.md (این سند)
```

نکته: commit a0df128 در جریان push — با مجوز مالک — کل کار معلق V1.8 (کد، تست،
migration، مستندات) را نیز به مخزن رساند؛ این پیش‌نیاز مکانیکی اجرای Actions بود.

## 10. Final Gate

```text
T-7 CI RUN FAILED — DIAGNOSTIC FOLLOW-UP REQUIRED
   (Classification: D/E — composer.lock platform vs workflow PHP matrix)
   Proposed minimal fix for next phase: matrix php '8.4' (یا هر دو 8.3+8.4 با
   resolution per-version) — تأیید مالک در فاز fix.
```

---

## FINAL SAFETY REPORT

```text
Production mutation: NO
Production schema mutation: NO
Production data mutation: NO

Migration files modified: NO
Application implementation modified: NO
Test implementation modified: NO
CI workflow modified: NO
```

**STOP** — طبق §۱۶: هیچ اصلاحی انجام نشد. فاز بعدی: diagnostic/fix هدفمند Gate D/E.
