# V1.8 — ENVIRONMENT RECOVERY & VERIFICATION REPORT

**تاریخ:** 2026-09-23
**نوع فاز:** Recovery + Read-only Verification — **صفر تغییر کد · صفر Migration · صفر تست · صفر mutation**

---

## ۱. Recovery Summary

```text
یافته شده:
  PostgreSQL 18.6 از طریق FlyEnv 4.18.3 مدیریت می‌شود (PHP Runtime Environment محلی).
  پس از ری‌استارت محیط، سرویس PostgreSQL متوقف بود (Connection refused :5432).

بازیابی:
  ✅ pg_ctl start — گزینهٔ موجود و غیرمخرب (بدون recreate / volume removal / initdb / config change)
  ⚠️ pg_ctl در foreground اجرا شد و shell 90s timeout گرفت — اما سرویس با موفقیت بالا آمد
     (خروجی قبلی start: "waiting for server to start... done · server started")
  ✅ pg_isready → accepting connections · exit=0 · PID 14512 روی 127.0.0.1:5432

عملیات مخرب: هیچ (هیچ DROP/CREATE/reset/initdb انجام نشد)
دیتابیس‌ها دست‌نخورده باقی ماندند — فقط inspection read-only
```

**Runtime شناسایی‌شده:**

| جزء | مقدار |
|---|---|
| Environment Manager | FlyEnv 4.18.3 (`C:\PhpWebStudy\FlyEnv\FlyEnv.exe`) |
| PostgreSQL Binary | `C:\PhpWebStudy-Data\env\postgresql\bin\` (v18.6) |
| Data Directory | `C:\PhpWebStudy-Data\server\postgresql\postgresql18` (PG_VERSION = 18) |
| Service Manager | نیست — FlyEnv چرخهٔ lifecycle را مدیریت می‌کند (Windows Service نصب نشده) |

---

## ۲. PostgreSQL Status

```text
Reachable: YES  (pg_isready → accepting connections · exit=0)
Version:   PostgreSQL 18.6 on x86_64-windows
Host:      127.0.0.1
Port:      5432
Runtime:   FlyEnv 4.18.3 (مستقل، بدون Windows Service)
```

---

## ۳. Production Database Verification

```text
Database:             tms
current_database():   tms
Migration status:     32/32 Ran · Batch {1:23, 2:1, 3:8} · Pending = 0   ✅
Tables:               35        ✅ (مطابق گزارش V1.8)
Triggers:             4         ✅
Functions:            3         ✅ (assert_stage_weight_locked · assert_stage_approval_immutable · assert_history_not_deletable)
CHECK constraints:    40        ✅
Foreign keys:         49        ✅
UNIQUE constraints:   14        ✅
supersedes UNIQUE:    0         ✅ (T-1 = ACCEPTED AS-IS به‌صورت ساختاری تأیید شد)
system_settings:      5         ✅
Size:                 1.57 MB
```

**هیچ انحراف ساختاری از آخرین وضعیت تأییدشده (2026-09-22) وجود ندارد.**

---

## ۴. Production Data Sanity

| جدول | Baseline تاریخی | مشاهده‌شده فعلی | اختلاف |
|---|---|---|---|
| tasks | 3 | **3** | صفر |
| projects | 1 | **1** | صفر |
| users | 8 | **8** | صفر |
| system_settings | 5 | **5** | صفر |

**هیچ تغییر داده‌ای از فاز Production Migration رخ نداده است.**

---

## ۵. Testing Database Verification

```text
Database:             tms_testing
current_database():   tms_testing   ✅
Reachable:            YES
Tables:               35 (ساختار مهاجرت‌شده از فازهای قبل)
```

**صفر mutation** — هیچ migrate/fresh/refresh/reset/wipe/seed/test اجرا نشد.

---

## ۶. Test Safety Gate

```text
Test DB safety gate:   PRESENT  (tests/TestCase.php — T-6)
Rule:                  نام واقعی دیتابیسِ متصل (از config واقعی فریمورک خوانده می‌شود)
                       باید به "testing" ختم شود؛ در غیر این صورت Suite قبل از
                       هر RefreshDatabase-driven reset fail می‌شود
Observed test DB:      tms_testing  (phpunit.xml: DB_CONNECTION=pgsql · DB_DATABASE=tms_testing)
Enforcement level:     ENFORCED برای مسیر test-driven reset ·
                       DOCUMENTED برای دستورات artisan دستی (docs/DATABASE_SAFETY.md)
```

---

## ۷. Repository Integrity

```text
Migration files changed:     0   (git diff -- database/migrations = خالی · ۳۲ فایل immutated)
Application files changed:   0   (توسط این فاز)
Test files changed:          0   (توسط این فاز)
Config files changed:        0
CI files changed:            0
Documentation files changed: 1 جدید (این گزارش)
```

**نکته (pre-existing، ناشی از این فاز نیست):** ۲۸ فایل tracked از فازهای قبلی V1.8 در working tree
تغییر یافته‌اند و ۹ فایل V1.8 migration هنوز untracked هستند — این وضعیت از فازهای قبل باقی
مانده و در همهٔ گزارش‌های قبلی هم ثبت شده است. **این فاز هیچ فایلی را تغییر نداد** به‌جز همین گزارش.

**Laravel config (بدون secret):**

```text
DB_CONNECTION:  pgsql
DB_HOST:        127.0.0.1
DB_PORT:        5432
DB_DATABASE:    tms           (app default)  ✅
DB_USERNAME:    root
DB_PASSWORD:    CONFIGURED (بدون نمایش)
phpunit.xml:    DB_CONNECTION=pgsql · DB_DATABASE=tms_testing   ✅
```

---

## ۸. Test Suite Status

```text
Previous verified baseline:   203 passed · 591 assertions · 0 failed · 2 deprecated
                              (2026-09-22 — آخرین اجرای معتبر)

Current suite:                NOT RUN — environment verification phase
```

طبق دستور فاز، Suite اجرا نشد تا ابتدا زیرساخت تأیید شود. Timeout فاز قبل (>300s) معتبر نبود
و نتیجه‌ای از آن استخراج نشد.

---

## ۹. Safety Report

```text
Production mutation:      NO
Testing DB mutation:      NO
Schema mutation:          NO
Data mutation:            NO
Migration execution:      NO
Seeder execution:         NO
CI implementation:        NO
Application implementation: NO
```

تنها عملیات انجام‌شده: **Start** سرویس موجود PostgreSQL (غیرمخرب، بدون recreate) + همهٔ
بازرسی‌ها read-only (`migrate:status` · `db:show` · SELECT روی catalog و counts).

---

## ۱۰. Environment Classification

```text
ENVIRONMENT VERIFIED
```

تمام معیارهای §۱۴ برآورده شد: PostgreSQL reachable · v18.6 · tms reachable ·
current_database()=tms · 32/32 migrations · ساختار 35/4/3/40/49/14 تأیید ·
tms_testing reachable · current_database()=tms_testing · Test safety gate PRESENT ·
migration files unchanged · repository بدون تغییر از این فاز.

---

## ۱۱. Blockers / Deviations

```text
1. pg_ctl start در foreground اجرا شد و shell timeout گرفت (90s) — اما سرویس بالا آمد و
   در ادامه تأیید شد. خطای ابزاری، نه مسدودکننده.
2. FlyEnv یک Windows Service نیست — پس از هر ری‌استارت سیستم، PostgreSQL باید دوباره
   از FlyEnv (یا pg_ctl) بالا آید. این محدودیت محیطی است، نه نقص مخزن.
3. هیچ blocker مخرب / هیچ انحراف ساختاری / هیچ تغییر داده‌ای یافت نشد.
```

---

## ۱۲. Next-Step Gate

```text
READY FOR T-7 CI IMPLEMENTATION PHASE
```

*(پیشنهاد spec از قبل آماده است: `docs/V1.8_NEXT_PHASE_DECISION_AND_IMPLEMENTATION_PLAN.md §۵.۱`.)*
**هیچ پیاده‌سازی‌ای در این فاز انجام نشد** — طبق دستور STOP بعد از REPORT.
```
```
