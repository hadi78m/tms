# MEMORY.md - حافظه پروژه

## اطلاعات پروژه
- **نام**: سامانه مدیریت وظایف (TMS - Task Management System)
- **نوع**: وب‌اپلیکیشن عملیاتی مدیریت وظایف قراردادها، گردش‌کار تاییدات، SLA و اسناد
- **فریم‌ورک**: Laravel 13 / Tailwind CSS (RTL) / PostgreSQL / Persian Datepicker
- **نسخه**: 1.0.0

## قوانین پروژه
- پایگاه داده تستی و اصلی PostgreSQL است؛ استفاده از SQLite in-memory به علت محدودیت‌های ساختاری و انطباق انواع ممنوع است.
- احراز هویت اختصاصی بر پایه نام کاربری (کدملی ۱۰ رقمی) و کلمه عبور صورت می‌پذیرد.
- تمام تاریخ‌های سیستم و ورودی/خروجی‌ها باید به فرمت شمسی (جلالی) باشند و در دیتابیس به صورت میلادی استاندارد ذخیره گردند.

## وضعیت تسک‌ها

### در حال انجام
- تحویل نسخه پایدار و آماده‌سازی برای مراحل استقرار نهایی

### در انتظار
- [ ] توسعه قابلیت‌های تکمیلی و یکپارچه‌سازی API در صورت نیاز

### انجام شده
- [x] 2026-09-18 پیاده‌سازی فاز S0 و Foundation دامین.
- [x] 2026-09-18 توسعه فاز S1 (Task Core) و تست‌ها.
- [x] 2026-09-18 توسعه فاز S2 (TaskAssignment).
- [x] 2026-09-18 رفع تمامی باگ‌های Constraintهای PostgreSQL در تست‌های سرویس‌ها و سبز شدن کامل Feature تست‌ها.
- [x] 2026-09-18 پیاده‌سازی فازهای S3 تا S6 و تست‌های یکپارچگی (Completed V1).
- [x] 2026-09-18 توسعه کامل لایه کاربری و داشبورد فاز V1.1 (Blade + Tailwind CSS RTL، کنترلرهای وب، DTOها، Spatie Roles، و پاس شدن ۶۴ تست سراسری).
- [x] 2026-09-18 پیاده‌سازی کامل فاز V1.2 (مدیریت کاربران، انتساب نقش‌های Spatie، ویوهای RTL Blade، روت‌های ادمین و پاس شدن ۶۹ تست سراسری).
- [x] 2026-09-19 تکمیل فاز V1.3 (گردشکار تایید دو مرحله‌ای): افزودن ۴ نقش جدید (employer، project_manager، management، viewer)، وضعیت SupervisorApproved، به‌روزرسانی State Machine، migration جدید، بازنویسی ApprovalService با recordTechnicalApproval و recordFinalApproval، تست‌های جدید (۳۱ تست دامین + ۶ تست workflow — همه سبز). ثبت فازهای V1.3–V1.6 در roadmap.
- [x] 2026-09-19 تکمیل فازهای V1.4 تا V1.6 (هش اسناد و ادعاها، ساب‌تسک‌ها و مسدودسازی وابستگی، تنظیمات داینامیک و خروجی گزارش‌ها).
- [x] 2026-09-20 یکپارچه‌سازی و تبدیل جامع تمام تاریخ‌های پروژه و سامانه به هجری شمسی (جلالی): ویوهای بلید، ستون‌های جداول، فرم‌های ثبت وظایف و مدارک با persianDatepicker، تبدیل خودکار درخواست‌ها در Request، گزارش‌های CSV، و پاس شدن ۱۰۰٪ تمامی ۱۰۲ تست پروژه.
- [x] 2026-09-20 تکمیل فاز تثبیت وب V1.7 (Web Stabilization Phase): اصلاح انتساب تسک (`TaskAssignmentService::assign` و نگاشت دقیق DTO)، اصلاح متد ارسال تسک (`TaskService::submitForReview` و توقف SLA حل)، بازنویسی نمایش وضعیت SLA در صفحه وظیفه بر مبنای `slaRecords`، تغییر روت حذف وابستگی به `POST` طبق قوانین OWASP، ایجاد ۱۲ تست وب در `WebTaskStabilizationTest` و پاس شدن ۱۰۰٪ کلیه ۱۱۴ تست سامانه در PostgreSQL.

## یادداشت‌ها
- تست‌ها مستقیماً بر روی پایگاه‌داده PostgreSQL اجرا و اعتبارسنجی می‌شوند.
- سیستم احراز هویت اختصاصی بر پایه شناسه کاربری و کدملی ۱۰ رقمی پیاده‌سازی شده است.
- تبدیل تاریخ شمسی به میلادی از طریق `prepareForValidation` در لایه FormRequestها مدیریت می‌شود و لایه دیتابیس همواره دیتای تمیز و استاندارد دریافت می‌کند.

